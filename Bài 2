#include <bits/stdc++.h>

using namespace std;

int main (){
	int t;
	cin >> t;
	while(t--){
		int n;
		cin >> n;
		int d=0, c=n%10;
		while(n>0){
			d=n%10;
			n/=10;
		}
		if(d==c) cout << "Yes";
		else cout << "No";
		cout << endl;
	}
}
